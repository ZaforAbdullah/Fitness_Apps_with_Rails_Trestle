<%= form_with(model: goal, local: true) do |form| %>
  <% if goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goal.errors.count, "error") %> prohibited this goal from being saved:</h2>

      <ul>
        <% goal.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :goal_type %>
    <%= form.text_field :goal_type %>
  </div>

  <div>
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :target_value %>
    <%= form.number_field :target_value %>
  </div>

  <div>
    <%= form.label :current_value %>
    <%= form.number_field :current_value %>
  </div>

  <div>
    <%= form.label :achieved %>
    <%= form.check_box :achieved %>
  </div>

  <div>
    <%= form.label :target_date %>
    <%= form.date_field :target_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
